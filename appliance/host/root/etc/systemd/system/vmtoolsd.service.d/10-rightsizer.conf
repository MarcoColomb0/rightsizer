# Report the appliance's name to vCenter. Flatcar re-links /etc/vmware-tools
# to a read-only directory on every start, so VMware Tools reads a separate
# config file that rightsizer-host keeps up to date.
[Service]
ExecStart=
ExecStart=/usr/bin/vmtoolsd -c /etc/rightsizer/vmware-tools.conf
